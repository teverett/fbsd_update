# fbsd_update
FreeBSD update ansible playbook

## Usage

sh runall.sh

* An account with the name `ansible` is required and the account must be a member of the `wheel` group
* You will need to ssh to each server first, to ensure that ssh keys are accepted

## Prerequisites

* Ansible
  
